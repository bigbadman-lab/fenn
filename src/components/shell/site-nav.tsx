/**
 * Former homepage-only world nav.
 * Replaced by navigable landmarks inside the ASCII world map.
 * Kept as a no-op export so any stale imports fail closed.
 */
export function SiteNav() {
  return null;
}
